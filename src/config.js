const config = {
    development: {
        apiEndpoint: 'http://localhost:3000/api/v1', // Update with your local API endpoint
    },
    production: {
        apiEndpoint: 'https://your-api-domain.com/api', // Update with your hosted API endpoint
    },
};

export default config;
